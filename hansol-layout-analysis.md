# 한솔그룹형 회사소개·하단 구성 분석

## 첨부 하단 레퍼런스 분석

제공된 가로형 하단 이미지는 `1,917×518px`이며, 좌측에만 정보가 밀도 있게 놓이고 나머지 공간은 의도적으로 넓게 비워 둔 흰색 푸터다. 첫 두 타일의 겹침 구간을 확인한 결과, 로고·저작권·정책 링크는 모두 화면 좌측 약 40px 안쪽에 정렬되어 있고 중앙과 우측은 시각적 호흡을 위해 비어 있다.

| 순서 | 좌측 배치 요소 | 구현 반영 |
| --- | --- | --- |
| 1 | 크게 보이는 워드마크/심볼 | 현재 구조시공 심볼과 이름을 넉넉한 크기로 배치 |
| 2 | 짧은 저작권 문구 | 브랜드명·저작권·연락처 보조 문구로 대체 |
| 3 | 작은 정책·안내 링크 | 개인정보처리방침·이용약관·상담 안내를 수평 배치 |

하단은 어두운 회사 정보 영역이 아니라, 넓은 흰 공간과 작은 검정 텍스트를 기반으로 하는 미니멀한 브랜드 엔딩으로 재해석한다.

중앙의 두 타일은 비어 있는 흰색 여백이며, 마지막 타일의 우측 하단에는 얇은 검정 테두리의 위쪽 화살표 버튼이 있다. 따라서 새 하단 영역에는 좌측 정렬 브랜드 정보와 우측 하단의 `맨 위로` 버튼만 두고, 다른 장식 요소는 추가하지 않는다.

## 회사소개 패널 적용 기준

한솔그룹의 하단 관심 영역은 큰 제목 아래에 네 개의 세로 패널을 끊김 없이 붙여 놓는다. 모든 패널은 사진 위에 낮은 채도의 검정 오버레이를 적용하고, 세로 영문 라벨과 하단의 굵은 흰색 한글 제목만 배치한다. 이 시각 언어를 현재 회사소개에 적용하되, 원본의 사진·명칭·카피는 사용하지 않는다.

현재 홈에는 `회사소개 → 공지사항·온라인상담 → 최종 문의안내 → 미니멀 브랜드 푸터` 순서로 배치한다. 기존의 홈 지도·갤러리 프리뷰·중간 전화 밴드는 제거하고, 실제 갤러리와 위치 안내는 상단 메뉴의 독립 페이지에서 유지한다.

## 패널 호버 전환 기준

비활성 패널은 저채도·어두운 상태로 유지하고, 실제 포인터가 진입한 패널만 컬러가 복귀하도록 한다. 활성 패널은 위쪽으로 약 34px 떠오르며, 이미지가 소폭 확대되고, 우측 하단의 흰색 화살표 사각 버튼이 확장된다. DOM 이벤트는 CSS `:hover` 상태를 만들지 않으므로, 시각 검증은 실제 마우스 이동으로 진행한다.

현재 브라우저 화면에서 4개 패널은 회사소개 헤드라인 아래의 중앙 영역에 한 줄로 노출된다. 첫 패널의 중앙 지점으로 실제 마우스를 이동해 CSS `:hover` 상태를 검증한다.

실제 호버 검증 결과 첫 패널은 `-34px` 위로 이동했고, 이미지 필터는 `grayscale(0) saturate(1.04)`로 전환됐으며, 화살표 버튼은 `72×72px`·불투명도 `1` 상태로 확장됐다. 나머지 패널은 저채도 상태를 유지한다.

키보드로 첫 패널에 포커스를 옮겼을 때도 동일하게 `-34px` 부상·컬러 복귀·72px 화살표 확장 상태가 유지된다.
