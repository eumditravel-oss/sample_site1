# 대메뉴 이동 및 좌측 서브내비게이션 검증 메모

상단 홈 화면에서 `회사소개` 대메뉴를 실제로 클릭했을 때 URL이 `/company`로 변경되어 첫 하위 페이지가 열리는 것을 확인했다. 회사소개 페이지에는 좌측에 `SECTION / 회사소개` 레이블과 인사말·오시는길 링크를 둔 라인 기반 서브내비게이션이 표시되며, 기존의 큰 전화 블록은 제거됐다.

현재 클릭 직후의 화면에서 상단 드롭다운은 아직 표시될 수 있어, 다음 단계에서 상태가 자동으로 닫히는지 확인하고 필요 시 이동 전에 닫히도록 순서를 보완한다.

대메뉴 클릭 뒤 포인터가 동일한 헤더 위치에 계속 머물면서 `mouseenter`가 다시 발생해 드롭다운이 재열리는 상태를 확인했다. 이제 대메뉴 클릭 시에는 헤더 밖으로 실제로 마우스가 나갈 때까지 호버 재열림을 억제하고, 마우스 이탈에서만 억제 상태를 해제하도록 보완했다.

각 라우트가 독립적인 `SiteFrame`을 렌더링해 헤더가 재마운트되므로, 호버 억제 상태는 컴포넌트 내부가 아닌 모듈 공통 상태로 유지한다. 이에 따라 클릭 후 새 페이지의 헤더가 마운트되어도 포인터가 헤더 밖으로 나가기 전에는 드롭다운이 다시 열리지 않는다.

최종 검증에서 `회사소개` 대메뉴 클릭은 `/company`로 정상 이동했으며, 새 페이지의 헤더에는 2차 드롭다운 링크가 더 이상 남지 않는 것을 확인했다.
