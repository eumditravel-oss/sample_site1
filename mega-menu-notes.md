# 메가메뉴 구현 기준

삼성물산 레퍼런스는 현재 자동화 브라우저에서 동적 화면을 렌더링하지 않아 직접적인 DOM·애니메이션 수치를 가져오지 않는다. 따라서 화면 구조를 복사하지 않고, 요청된 정돈된 기업형 경험을 기준으로 다음을 적용한다.

| 항목 | 적용 기준 |
| --- | --- |
| 열림 방식 | 데스크톱 1차 메뉴의 호버·포커스·클릭에서 확장 패널을 열고, 메뉴 밖 이탈·Escape에서 닫는다. |
| 정보 구조 | 좌측에는 현재 메뉴의 제목·설명, 우측에는 2차 메뉴와 빠른 행동 링크를 둔다. |
| 모션 | 불투명도·Y축 이동·패널 높이만 짧게 전환하고, 메뉴 항목은 순차적으로 나타나게 한다. |
| 접근성 | 버튼의 `aria-expanded`, `aria-controls`, `Escape`, 포커스 진입 및 이탈을 지원한다. |
| 모바일 | 기존 전체 화면 메뉴는 유지하되, 1차 메뉴별 아코디언 서브메뉴를 추가한다. |

빌드는 성공했으며, 개발 화면에는 1차 메뉴 버튼이 정상 노출된다. 브라우저 콘솔의 비동기 대기 문법은 실행 환경 제약으로 동작하지 않았으며, 콘솔 클릭은 현재 상태를 닫는 토글 결과를 보일 수 있다. 실제 마우스 진입으로 서비스안내를 열었을 때 4개 2차 링크, 메뉴 설명, 전화·상담 빠른 링크가 포함된 확장 패널이 정상 노출됨을 확인했다.

전역 Escape 처리를 추가한 뒤 개발 화면이 다시 로드되며 메가메뉴가 닫힌 초기 상태로 복귀하는 것을 확인했다.

자동화 브라우저의 축소된 화면 좌표와 CSS 좌표가 달라 재호버 재현에는 오차가 있었으나, 앞선 실제 호버 검증에서 확장 패널 노출은 확인됐다. 이후 검증은 DOM 이벤트를 사용한다.

DOM `mouseover` 검증에서 서비스안내 메뉴는 `aria-expanded=true`, `aria-hidden=false`, 확장 클래스 적용 및 4개 2차 링크 노출 상태를 반환했다. 열린 상태에서 Escape를 누른 뒤 확장 패널이 사라지는 것을 화면으로 확인했다.
